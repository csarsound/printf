#ifndef MAIN_TYPE
#define MAIN_TYPE

/*LITBS*/
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

/*PROTOTYPES*/

int _printf(const char *format, ...);
char *conv_c(void*);
char *conv_s(void*);
char *conv_%(void*);

#endif/*MAIN_TYPE*/
